第一阶段：生成1~255条IP命令
1.  新建文件
    ```
    vim result.txt
    ```
2.  写第一行
    按 `i` 输入：
    ```
    ssh root@10.64.198.1 > 1.log
    ```
    按 `Esc` 退出插入模式。
3.  录制宏（关键）
    依次按：
    `qq` → `yy` → `p` → `$` → `F1` → `Ctrl+a` → `q`
4. 批量生成
    按 `gg` 回到首行，输入：
    ```
    254@q
    ```
第二阶段：分组修改日志文件
在Vim命令模式下，依次输入（每输完一条按回车）：
```vim
:51,100s/> 1\.log/> 2.log/g
:101,150s/> 1\.log/> 3.log/g
:151,200s/> 1\.log/> 4.log/g
:201,250s/> 1\.log/> 5.log/g
:251,255s/> 1\.log/> 6.log/g
第三阶段：检查+保存
1.  按 `Esc` 后输入 `:set nu` 显示行号，核对分组。
2.  保存退出：
    ```vim
    :wq
    ``
